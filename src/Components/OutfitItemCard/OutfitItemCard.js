import '../OutfitItemCard/OutfitItemCard.css';

const OutfitItemCard = ({ products, outfitItems, deleteItemFromOutfit }) => {
  const idsOfOutfitItems = Object.values(outfitItems);
  const productsForOutfitItems = outfitItems.

  return (

  );

}

export default OutfitItemCard;